[package]
name = "xtask"
version = "0.0.1"
edition = "2021"
rust-version = "1.74.1"
publish = false
description = "Host-only scaffold for a future fARM64 mechanical-table generator; not part of the published library."

[dependencies]
# Host-only tool: free to use std and host-only dependencies. Keep the
# dependency surface empty while the generator remains a scaffold.
